import React from 'react';
import { PartyPopper } from 'lucide-react';

const HolidayIcon = (props: React.SVGProps<SVGSVGElement>) => (
  <PartyPopper size={20} {...props} />
);

export default HolidayIcon;