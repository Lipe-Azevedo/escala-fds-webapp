import React from 'react';
import { ArrowRightLeft } from 'lucide-react';

const SwapIcon = (props: React.SVGProps<SVGSVGElement>) => (
  <ArrowRightLeft size={20} {...props} />
);

export default SwapIcon;