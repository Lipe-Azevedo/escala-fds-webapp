import React from 'react';
import { MessageSquarePlus, LucideProps } from 'lucide-react';

const MessageSquarePlusIcon = (props: LucideProps) => (
  <MessageSquarePlus {...props} />
);

export default MessageSquarePlusIcon;